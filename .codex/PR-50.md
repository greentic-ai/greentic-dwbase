# .codex/PR-50.md — WIT/Schema Stability + CI: wasm32-wasip2 + Pack Build on PRs

## Goal
Prevent drift between WIT, component schemas, and pack artifacts by enforcing checks in CI.

## Deliverables
- Add CI jobs (on PR/push):
  - `cargo build -p component-dwbase --target wasm32-wasip2`
  - `./packs/dwbase-memory/build.sh`
  - validate `crates/component-dwbase/component.manifest.json` is valid JSON
- Add a small “schema sync” check:
  - ensure manifest operation list matches WIT exports (best-effort script)
- Document how to run these checks locally.

## Acceptance
- PRs fail fast if WIT/schema/pack builds drift.

