# .codex/PR-02.md — Workspace + Crate Skeletons

## Goal
Create the Rust workspace layout for DWBase and stub crates so everything compiles (even if empty).

## Deliverables
Create:
- Root `Cargo.toml` workspace
- Crate directories + minimal `Cargo.toml` + `src/lib.rs` or `src/main.rs`

Workspace layout:
- crates/dwbase-core
- crates/dwbase-engine
- crates/dwbase-storage-sled
- crates/dwbase-vector-hnsw
- crates/dwbase-stream-local
- crates/dwbase-security
- crates/dwbase-embedder-dummy
- crates/dwbase-node (bin)
- crates/dwbase-wit-host
- crates/dwbase-wit-guest
- crates/dwbase-cli (bin)
- crates/dwbase-metrics
- crates/dwbase-swarm (stub)

## Constraints
- Keep dependencies minimal (serde, thiserror, anyhow only when needed).
- No functional logic yet. Just structure + compilation.

## Acceptance Criteria
- `cargo build` succeeds for the whole workspace.
- Each crate has a brief README header comment in `lib.rs` describing intent.