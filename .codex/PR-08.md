# .codex/PR-08.md — StreamEngine: Local Observe Streams (dwbase-stream-local)

## Goal
Implement observe() functionality via local pub/sub and poll semantics.

## Deliverables
In `crates/dwbase-stream-local`:
- `LocalStreamEngine` implementing `StreamEngine`
- `start_subscribe(filter)->handle`
- `poll(handle,max)->Vec<Atom>`
- `stop(handle)`

## Constraints
- Use `tokio::sync::broadcast` or `mpsc` with per-subscriber filtering.
- Poll-based interface (no WASM streams).

## Tests
- subscribe then publish then poll yields expected atoms
- filter respected