# .codex/PR-12.md — Node Binary: dwbase-node (HTTP or gRPC)

## Goal
Create a runnable local DWBase node that hosts the engine and exposes network APIs.

## Deliverables
In `crates/dwbase-node` (binary):
- config file support (toml)
- engine wiring:
  - sled storage
  - hnsw vector
  - local stream
  - security gatekeeper
  - dummy embedder default (pluggable)
- API:
  - pick **gRPC (tonic)** or **HTTP (axum)**, implement:
    - remember
    - ask
    - replay
    - list-worlds
- minimal README with how to run

## Acceptance Criteria
- `cargo run -p dwbase-node -- --config ./config.toml` starts and serves.
