# Default pack configuration for component-dwbase (safe-by-default).
#
# Greentic hosts can load this env file (or map fields into their own config system).

# Data root (persisted state). Using a relative path keeps the pack self-contained.
DWBASE_DATA_DIR=./data/dwbase

# Tenant scoping (PR-36 safe defaults)
DWBASE_TENANT_ID=demo
DWBASE_ENFORCE_TENANT_NAMESPACE=1

# Conservative safety limits
DWBASE_MAX_PAYLOAD_BYTES=65536
DWBASE_IMPORTANCE_CAP=0.7
DWBASE_ALLOWED_KINDS=observation,reflection

# NATS (optional; presence/replication/observe broadcast are feature-gated)
NATS_URL=nats://127.0.0.1:4222

# Optional: enable swarm features for demo environments
# DWBASE_SWARM_ENABLE=1
# DWBASE_SUBSCRIBE_WORLDS=tenant:demo/*

# Optional: cross-node observe over NATS
# DWBASE_OBSERVE_NATS_BROADCAST=1
# DWBASE_OBSERVE_NATS_SUBSCRIBE=1

